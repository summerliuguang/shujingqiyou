# assets/music/ —— 各游戏 BGM（本仓库不包含音频文件）

公开仓库不分发任何音频（本目录除本 README 外已被 `.gitignore` 排除）。
本地运行时把 `.mp3` 放进本目录即可，文件名需与游戏数据包
`manifest.json` 的 `audio.bgm` 字段一致。

选曲建议使用可再分发授权的音乐（例如 Kevin MacLeod / incompetech，
CC BY 4.0），并在本目录保留 `ATTRIBUTION.md` 署名文件，格式：

    曲名 — 音乐人 (来源 URL), 授权协议（如 CC BY 4.0）
